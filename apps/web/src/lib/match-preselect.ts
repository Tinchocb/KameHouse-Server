/** sessionStorage key para llegar desde la paleta con un archivo preseleccionado.
 *  Módulo mínimo sin dependencias: importarlo no arrastra componentes pesados
 *  (p. ej. la paleta de comandos lo usa sin importar el manual de match TMDB). */
export const MATCH_PRESELECT_KEY = "kamehouse-match-preselect"
