version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "43000:43000"
    volumes:
      - kamehouse-data:/data
    environment:
      - PORT=43000
      - GIN_MODE=release
    restart: unless-stopped

volumes:
  kamehouse-data:
